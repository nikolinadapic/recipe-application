import React from 'react';
import Spinner from '../UI/Spinner/Spinner';

const Home = props => {
    return (
        <div style={{height: '80vh'}}>
        <h1>Home</h1>
        <Spinner />
        </div>
    );
}

export default Home;